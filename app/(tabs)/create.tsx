import { View } from 'react-native'

// This file exists to make Expo Router happy, but the tab button
// will navigate to /create-post instead
export default function CreateTabPlaceholder() {
  return <View />
}
